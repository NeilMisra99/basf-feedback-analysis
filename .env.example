# Feedback Analysis App - Environment Configuration
# Copy this file to .env.local (for local dev) or configure in production

# =============================================================================
# LOCAL DEVELOPMENT CONFIGURATION
# =============================================================================

# Flask Configuration
SECRET_KEY=dev-secret-key-change-in-production
FLASK_ENV=development

# Azure Text Analytics (Required)
AZURE_TEXT_ANALYTICS_KEY=your-azure-text-analytics-key
AZURE_TEXT_ANALYTICS_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# Azure Speech Services (Required)
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=your-azure-region

# OpenAI API (Required)
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o

# Note: PORT defaults to 5001 for local dev, 80 for production (Container Apps)
# REACT_APP_API_URL defaults to http://localhost:5001/api/v1 for local dev
# Only set these if you need to override defaults:
# PORT=5001
# REACT_APP_API_URL=http://localhost:5001/api/v1
# CORS_ORIGINS=http://localhost:3000

# =============================================================================
# PRODUCTION CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# BACKEND (Azure Container Apps)
# -----------------------------------------------------------------------------
# All backend environment variables are configured via GitHub Actions deployment.
# The Container App gets environment variables set automatically during deployment.
# No manual configuration needed in Azure Portal.

# -----------------------------------------------------------------------------
# GITHUB ACTIONS SECRETS (Repository Settings → Secrets and variables → Actions)
# -----------------------------------------------------------------------------

# Container Registry Secrets:
# REGISTRY_LOGIN_SERVER=your-registry-xxxxx.azurecr.io
# REGISTRY_USERNAME=your-registry-username  
# REGISTRY_PASSWORD=your-registry-password

# Azure Deployment Secrets:
# AZURE_CREDENTIALS=<service-principal-json>
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...

# Application Environment Secrets:
# AZURE_TEXT_ANALYTICS_KEY=your-azure-text-analytics-key
# AZURE_TEXT_ANALYTICS_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
# AZURE_SPEECH_KEY=your-azure-speech-key
# AZURE_SPEECH_REGION=your-azure-region
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_MODEL=gpt-4o
# SECRET_KEY=your-secure-production-secret-key
# CORS_ORIGINS=https://your-frontend-app.azurestaticapps.net

# Frontend Deployment:
# AZURE_STATIC_WEB_APPS_API_TOKEN=<your-static-web-apps-token>
# REACT_APP_API_URL=https://your-backend-app.azurecontainerapps.io/api/v1